{
  "name": "zareshop-articles",
  "version": "1.0.0",
  "description": "Zareshop Articles - Full-stack article management system",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
